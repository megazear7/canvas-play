# Canvas Playground

## Setup
* Clone the repository
* `npm install`

## Local Development
* `npm start`
* http://localhost:8000

## Deployment
* `npm run build`
* `git commit -m "my commit message"`
* https://canvas.alexlockhart.me/
